[package]
name = "phase19-admission-receipt"
version = "0.1.0"
edition = "2021"
authors = ["Kenan AY"]
description = "Phase-19 bounded admission and receipt harness"

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
